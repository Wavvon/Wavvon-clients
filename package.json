{
  "name": "wavvon",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
